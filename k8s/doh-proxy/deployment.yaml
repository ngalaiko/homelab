apiVersion: apps/v1beta2
kind: Deployment
metadata:
  name: doh-proxy
  labels:
    app: doh-proxy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: doh-proxy
  template:
    metadata:
      labels:
        app: doh-proxy
    spec:
      imagePullSecrets:
      - name: github
      containers:
      - name: doh-proxy
        image: docker.pkg.github.com/ngalaiko/server/doh-proxy
        args:
        - --upstream-resolver=pihole-dns
        - --port=3000
        - --listen-address=0.0.0.0
        - --trusted
        ports:
        - containerPort: 3000
          name: http
