kind: Service
apiVersion: v1
metadata:
  name: openvpn
  labels:
    app: openvpn
spec:
  ports:
    - name: tcp
      port: 1194
      targetPort: tcp
      protocol: TCP
  selector:
    app: openvpn
